import Usuario from '../modelo/usuario.js';
import Cuenta from '../modelo/cuenta.js';

export const registro = (req, res) => { res.send('Registro') };

export const inicio_sesion = (req, res) => { res.send('Inicio de sesión') };
